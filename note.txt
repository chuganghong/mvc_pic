1.如何让已经关注过的人不出现在“增加朋友”列表中？解决方法有
（1）让单个“关注”按钮失效或显示“已经关注”。不足：若为关注的用户和已经关注的用户混合在一起，批量关注按钮是该有效还是失效呢？
（2）通过数据库操作让已经关注过的人不出现在“增加朋友”列表中。但是，SQL语句中的排除选择特别是大量数据的排除选择，我不会使用。
2.mvc_pic程序写到现在，出现了大量的重复代码，而且，实现一些功能，很容易，几条PHP语句，再加上几条SQL语句，让我觉得我现在写的这些代码毫无技术含量，只需肯华时间，就可以把这些功能全部实现。PHP就是这种无技术含量的语言还是其他的语言到了这种程度，都是如此呢？
不过，这也告诉我：PHP，重要的不止是实现某些功能，更重要的也许是，整个系统的架构（也就是整个程序的代码是否存在大量冗余代码、是否是可维护的）更加重要，还有和PHP有关的大量的其他内容，比如Memcached，都是更加重要的。
3.此程序写到这个程度，我不知道该如何写下去，应该开始重构吗？
4.私信功能：暂时只能实现一次给一个用户发信；一次给多个用户发信，我不知如何实现，难点是，当选择了多个收信人之后，又删掉某个收信人，此时，如何让收信人ID和收信人用户名保持一致？直接发送收信人用户名而不是ID，可以跳过此难点，但是，需要多一次数据库操作。